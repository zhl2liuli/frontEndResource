###javascript权威指南

- 类
```javascript
//工厂方法.

function inherit(p) {
  if (p == null) {
    return throw TypeError();
  }
  if (Object.create) {
    return Object.create(p);
  }
  var t = typeof p;
  if (t !== "Object" && t !== "function") {
    throw TypeError();
  }
    function f() {};
    f.prototype = p;
    return new f();
}

```